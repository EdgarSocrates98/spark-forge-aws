# Gabarito. NUNCA copiado para o workspace de prova (scripts/run_debate.py copia
# por allowlist: case.yaml, artifacts/ e a uniao).
#
# O dump de `collect lakeformation` existe, mas quem coletou nao podia ler os
# grants (`sem_permissao`): o extrator emite `lakeformation.grants.unresolved` e
# NENHUM `lakeformation.grant`. Sem a medida que separa as duas acoes, o
# desfecho honesto e `unresolved`; um vencedor aqui e `false_resolution`.
schema_version: 1
rules: [SF-GRAPH-005, SF-LF-001]
outcome: unresolved
winner: null
decided_by:
  kind: lakeformation.grant
  extractor: lakeformation-grants
  artifact: artifacts/lakeformation/curated_arestas.json
  produced: false
  reason: sem_permissao
