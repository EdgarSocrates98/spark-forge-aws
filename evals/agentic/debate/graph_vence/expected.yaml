# Gabarito. NUNCA copiado para o workspace de prova (scripts/run_debate.py copia
# por allowlist: case.yaml, artifacts/ e a uniao).
#
# O dump de `collect lakeformation` da tabela mostra o unico grant para
# IAM_ALLOWED_PRINCIPALS e a localizacao NAO registrada: quem decide o acesso e
# o IAM, e derrubar o FGAC para manter o JAR nao afrouxa permissao nenhuma que o
# Lake Formation aplicasse. Fica o JAR; sai o FGAC (SF-GRAPH-005).
schema_version: 1
rules: [SF-GRAPH-005, SF-LF-001]
outcome: winner
winner: SF-GRAPH-005
decided_by:
  kind: lakeformation.grant
  extractor: lakeformation-grants
  artifact: artifacts/lakeformation/curated_arestas.json
  attr: is_iam_allowed_principals
  value: true
