rules:
  - id: GAMA-GOV-001
    category: beta
    title: Regra com prefixo de outro dono
    requires_facts: [tf.attribute]
    when:
      all:
        - fact: tf.attribute
          where: {attrs.key: worker_type, attrs.value: G.8X}
    status: structural
    severity_default: P3
    runtime_scope: {glue: "*"}
    sources:
      - {origin: sintetico}
