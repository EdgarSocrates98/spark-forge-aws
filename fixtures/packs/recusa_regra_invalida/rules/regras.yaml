rules:
  - id: SEMF-GOV-001
    category: sem-fonte
    title: Regra sem fonte
    requires_facts: [tf.attribute]
    when:
      all:
        - fact: tf.attribute
          where: {attrs.key: worker_type, attrs.value: G.8X}
    status: structural
    severity_default: P3
    runtime_scope: {glue: "*"}
