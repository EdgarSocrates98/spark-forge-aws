{
  "runtime": {
    "glue_version": "5.0",
    "spark_version": "3.5.4",
    "aqe_default": true
  },
  "properties": [
    {
      "key": "spark.executor.memory",
      "current": {
        "value": "",
        "provenance": "unset",
        "evidence": []
      },
      "derived": {
        "value": 3072,
        "formula": "ceil(max_executor(peak_jvm_heap_bytes) / MiB)",
        "basis": {
          "worst_executor": "1",
          "executors_measured": 1,
          "peak_jvm_heap_bytes": 3221225472.0
        }
      },
      "safety": "REVIEW",
      "supported_in_runtime": true,
      "explanation": "Piso do pior executor medido no heap da JVM. Abaixo dele o heap nao cabe o que o run ja usou; acima, e escolha de quem aprova."
    }
  ],
  "refused": [
    {
      "reason": "no_shuffle_measured",
      "property": "spark.sql.shuffle.partitions",
      "detail": "Nenhum `spark.stage.shuffle` com `write_bytes` acima de zero. Zero particoes nao e configuracao, e um job sem shuffle nao tem o que paralelizar aqui. `sparkforge analyze event-log` produz a medida."
    },
    {
      "reason": "sem_process_tree",
      "property": "spark.executor.memoryOverhead",
      "detail": "O event log nao traz `ProcessTreePythonRSSMemory`, e sem o processo Python o pico fora do heap e metade da medida. Ligue `spark.executor.processTreeMetrics.enabled=true` (default `false`) e colete o event log de novo."
    },
    {
      "reason": "sem_footer",
      "property": "spark.sql.files.maxPartitionBytes",
      "detail": "Nenhum `parquet.row_group` com tamanho comprimido. `sparkforge collect parquet-footer --prefix <fonte>` e `sparkforge analyze parquet-footer` o produzem."
    },
    {
      "reason": "sem_explain_cost",
      "property": "spark.sql.autoBroadcastJoinThreshold",
      "detail": "Nenhuma estimativa de join. Gere o plano com `df.explain(\"cost\")` (ou `EXPLAIN COST`) e extraia com `sparkforge analyze plan`."
    },
    {
      "reason": "sem_tasks_por_executor",
      "property": "spark.speculation",
      "detail": "Nenhuma duracao de task nos facts. `sparkforge analyze event-log` a extrai de `SparkListenerTaskEnd`, com o executor de cada task."
    },
    {
      "reason": "sem_relacao_observada",
      "property": "spark.network.timeout",
      "detail": "Sem `spark.timeout.relation`: o run nao reportou `spark.executor.heartbeatInterval` e `spark.network.timeout` juntos em `spark.conf_effective`. `sparkforge analyze event-log` seguido de `sparkforge fuse` a produz."
    },
    {
      "reason": "sem_diagnostico_de_broadcast",
      "property": "spark.sql.broadcastTimeout",
      "detail": "Nenhum `spark.timeout.diagnosis` com categoria `broadcast`. Sem o broadcast estourando o limite nao tem o que sustentar; `wall_clock` e `heartbeat` nunca tem valor proposto (regra 15)."
    }
  ]
}
