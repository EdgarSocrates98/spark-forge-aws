[
  {
    "rule_id": "SF-LF-011",
    "schema_version": 1,
    "catalog_version": 1,
    "title": "O job falhou por permissão do Lake Formation, e a permissão que a operação exige não está no grant medido (ou na decisão de IAM, sob FGAC)",
    "severity": "P1",
    "confidence": "high",
    "status": "confirmed",
    "subject": {
      "type": "table",
      "symbol": "analytics.dim_cliente#write#lf"
    },
    "evidence": [
      "f_9a5010"
    ],
    "measured": {},
    "threshold": {},
    "runtime_scope": {},
    "explanation": "O log do run tem `Insufficient Lake Formation permission(s) on <recurso>` (`ERR-LF-001`), e o fact `lakeformation.missing_grant` cruzou essa falha com a operação que o código faz sobre o recurso. O que ele cruza depois depende do lado. No lado do Lake Formation, é o grant coletado por `collect lakeformation`. No lado IAM (escrita sob FGAC), o fact não lê grant: lê o registro da localização (`registered` e `resource_arn`, também de `collect lakeformation`), a versão do runtime pela matriz e a decisão de `iam:SimulatePrincipalPolicy` coletada por `collect iam-access`.\n\n**O lado muda o conserto.** Com `side: lf` (Full Table Access, ou leitura sob FGAC), falta permissão no grant do Lake Formation. Para job Spark do Glue que escreve sob FTA, a permissão é `ALL`, e não `INSERT`: *\"AWS Glue Spark jobs that write/delete data in Amazon S3 require AWS Lake Formation ALL permission.\"* Com `side: iam` (escrita sob FGAC em localização com `registered: false` medido), quem autoriza a escrita é o IAM do runtime role, e `denied_by` diz qual camada negou. Nesse caso, acrescentar uma statement só resolve quando `denied_by` é `implicit_deny`.\n\n**Qual decisão de IAM acusa.** Só a que fala pela tabela: `implicitDeny` no prefixo de objetos da localização (`<localização>/*`, onde a localização é o `--resource-arn` passado a `collect lakeformation`), ou `explicitDeny` num recurso que contém a tabela. Negação implícita em `*`, no bucket ou num objeto não acusa, porque uma policy escopada à tabela também nega ali. O fact do lado IAM carrega `registration_scope: exact_arn` e `caveat`: o `registered: false` veio do ARN exato da localização, e registro num prefixo pai não foi conferido. Se ele existir, o caso é o conflito declarado, e não falta de IAM. Carrega também `unchecked`: a fonte das ações (*\"Data targets require s3:ListBucket, s3:PutObject, and s3:DeleteObject permissions.\"*) não separa append de overwrite, e a escrita cobra `s3:PutObject` e `s3:DeleteObject`; `s3:ListBucket` (ação de bucket) e KMS (ações `kms:*` sob SSE-KMS, e a key policy) não são simulados.\n\n**O modo de escrita pode não ter sido lido.** Com `mode_unresolved: true` no fact, o modo vem de expressão que o parse não lê. A acusação vale assim mesmo, porque o que falta para `write` falta em qualquer modo. O fact não diz se o job faz append ou overwrite, e a regra também não diz.\n\n**O que a regra NÃO afirma:** que o job passa depois do grant. Outras partes do caminho (RAM share, key policy do KMS, bucket policy) não têm coletor, e a simulação de IAM não avalia policy de recurso. Escrita em localização registrada sob FGAC não chega aqui: é o conflito declarado da §6 de `knowledge/glue/lakeformation-fgac.md` e sai como recusa nomeada.",
    "proposed_change": [
      "Com `side: lf`: conceder ao principal do fact, sobre o recurso do fact, as permissões de `attrs.missing`. Confira antes se a tabela deve ser governada pelo Lake Formation (`SF-LF-008`), porque grant numa tabela aberta a `IAM_ALLOWED_PRINCIPALS` não muda nada.",
      "Com `side: iam`: o alvo não é o grant do Lake Formation, é a policy do runtime role (o `action.target` da regra nomeia só o lado LF, porque o bloco aceita um alvo). Ler `attrs.denied_by` antes de editar a policy. Se for `implicit_deny`, falta uma statement com `attrs.action` sobre `attrs.iam_resource`. Se for `explicit_deny`, `permissions_boundary` ou `service_control_policy`, acrescentar não resolve: é preciso achar a negação e decidir se ela sai. Antes, leia `attrs.caveat`: se a localização estiver registrada num prefixo pai, o caso é o conflito declarado e a policy não é o conserto."
    ],
    "expected_effect": "",
    "benchmark_ref": "",
    "risks": [
      "`ALL` concede também `DROP` e `ALTER` sobre a tabela. Sob FTA, a fonte exige `ALL` para escrever, e o custo dessa exigência é um raio maior que o da operação.",
      "A operação vem do código analisado. Alvo em variável não é resolvido pelo extrator de código, e nesse caso a operação sai recusada (`operacao_com_alvo_nao_resolvido`), não acusada. Quando o código só tem alvos literais e nenhum casa com o recurso da mensagem, a recusa é `operacao_nao_ligada_ao_recurso`.",
      "Sob FGAC, a localização é a que o operador passou ao coletor. Se ela for mais larga que a tabela (o bucket), uma policy escopada ao prefixo da tabela também nega em `<localização>/*`, e a acusação pode apontar uma falta que a tabela não tem."
    ],
    "tradeoffs": [
      "Conceder só o que falta mantém o menor privilégio. Trocar o modelo de acesso (FGAC ou FTA) muda quem vende a credencial, e é decisão de plataforma, não de job."
    ],
    "validation": [
      "Com `side: lf`, recoletar com `sparkforge collect lakeformation`; com `side: iam`, recoletar com `sparkforge collect iam-access --role-arn <runtime-role> --resource-arn <localização>/* --action <attrs.action>`, repetindo `--action` para cada ação que a operação exige (na escrita sob FGAC, append ou overwrite, `s3:PutObject` e `s3:DeleteObject`). Rodar `sparkforge fuse` de novo: o `lakeformation.missing_grant` do recurso precisa sumir.",
      "Reexecutar o job e confirmar que `ERR-LF-001` sai do log do run. Nenhum eixo de dado é exigido: a mudança é de permissão, não de conteúdo."
    ],
    "rollback": [
      "Revogar a permissão concedida (`aws lakeformation revoke-permissions`) ou remover a statement acrescentada ao role. O job volta a falhar como antes."
    ],
    "sources": [
      {
        "url": "https://docs.aws.amazon.com/lake-formation/latest/dg/lf-permissions-reference.html",
        "retrieved": "2026-09-21"
      },
      {
        "url": "https://docs.aws.amazon.com/glue/latest/dg/security-access-control-fta.html",
        "retrieved": "2026-09-21"
      },
      {
        "url": "https://docs.aws.amazon.com/glue/latest/dg/security-lf-enable-considerations.html",
        "retrieved": "2026-09-21"
      },
      {
        "url": "https://docs.aws.amazon.com/glue/latest/dg/getting-started-min-privs-job.html",
        "retrieved": "2026-09-25"
      }
    ],
    "action": {
      "kind": "security.verify_access_chain",
      "target": "lakeformation.grant",
      "direction": "investigate",
      "requires_absent": [],
      "moves": [],
      "depends_on": []
    }
  }
]
