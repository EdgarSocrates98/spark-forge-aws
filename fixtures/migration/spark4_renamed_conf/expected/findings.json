[
  {
    "rule_id": "SF-SPARK4-001",
    "schema_version": 1,
    "catalog_version": 1,
    "title": "Configuração do Spark que mudou de nome na versão 4 sobrevivendo com o nome antigo",
    "severity": "P2",
    "confidence": "high",
    "status": "structural",
    "subject": {
      "type": "source_location",
      "file": "job.py",
      "line": 1,
      "col": 0,
      "symbol": "",
      "snippet": ""
    },
    "evidence": [
      "f_c4e695",
      "f_04d3ff"
    ],
    "measured": {},
    "threshold": {},
    "runtime_scope": {
      "spark": ">=4.0.0"
    },
    "explanation": "As configurações de rebase de data e hora perderam o prefixo `legacy` no Spark 4.0 -- `spark.sql.legacy.parquet.int96RebaseModeInWrite` virou `spark.sql.parquet.int96RebaseModeInWrite`, e o mesmo vale para as variantes `datetimeRebaseMode` de Parquet e Avro; o codec Parquet `lz4raw` virou `lz4_raw`. O risco não é erro, é SILÊNCIO. O Spark ignora a chave que não conhece, a submissão passa, o job roda até o fim, e quem lê o código vê uma configuração que parece ativa e não está em vigor nenhuma. É o mesmo modo de falha de `fs.s3.consistent` no Glue 5 (`SF-MIG-002`), e ele é pior que uma quebra: uma quebra aparece, um rebase de timestamp que não aconteceu aparece meses depois, dentro do dado. Ver `knowledge/spark/spark4-migration.md` §2 para a tabela completa de nomes antigos e novos.",
    "proposed_change": [
      "Renomear a chave para o nome novo declarado em `attrs.renamed_to`, preservando o valor -- a semântica da configuração não mudou, só o nome.",
      "Onde a chave veio de um template ou de uma biblioteca compartilhada, corrigir na origem, para que o próximo job criado a partir dela não nasça com o nome morto.",
      "Conferir se o valor ainda é o desejado antes de renomear -- uma chave inerte pode estar carregando um valor que ninguém revisa desde que ela parou de ter efeito."
    ],
    "expected_effect": "",
    "benchmark_ref": "",
    "risks": [
      "Renomear a chave LIGA uma configuração que estava inerte. Se o job passou a depender do comportamento default enquanto a chave não valia, o rebase de timestamp volta a acontecer e a saída muda -- a mudança de resultado vem de a configuração passar a funcionar, não de um defeito.",
      "Uma chave `spark.sql.legacy.*` que NÃO está na tabela de renomeadas continua válida no Spark 4 (`timeParserPolicy`, `ctePrecedencePolicy`). Renomear por analogia de prefixo desliga configuração correta."
    ],
    "tradeoffs": [
      "Remover a chave em vez de renomeá-la é mais barato e assume o default do Spark 4; renomear preserva a intenção original de quem a escreveu. A escolha depende de a intenção ainda valer, e o catálogo não sabe disso.",
      "Deixar a chave antiga no código não quebra nada hoje e custa clareza para sempre -- o próximo leitor vai assumir uma garantia que não existe."
    ],
    "validation": [
      "Configuração efetiva do job no runtime alvo contém a chave NOVA com o valor esperado, lida de `spark.conf.get` ou da aba Environment da Spark UI, não do código-fonte.",
      "Para as chaves de rebase, contagem de linhas e agregado (mín, máx) da coluna de timestamp afetada, antes e depois, sobre o mesmo lote de entrada -- divergência mostra que a chave estava inerte e passou a valer.",
      "Contagem total de linhas e schema idênticos aos do runtime anterior."
    ],
    "rollback": [
      "Reverter para o nome antigo e reaplicar. A chave volta a ser inerte no Spark 4, então o rollback restaura o comportamento observado hoje -- documentar isso no commit, para que a inércia não seja lida como configuração ativa de novo."
    ],
    "sources": [
      {
        "url": "https://spark.apache.org/docs/4.1.1/sql-migration-guide.html",
        "retrieved": "2026-08-22"
      }
    ]
  }
]
