[
  {
    "rule_id": "SF-DQ-002",
    "schema_version": 1,
    "catalog_version": 1,
    "title": "Validação sem consequência",
    "severity": "P1",
    "confidence": "high",
    "status": "structural",
    "subject": {
      "type": "source_location",
      "file": "job.py",
      "line": 32,
      "col": 8,
      "symbol": "",
      "snippet": ""
    },
    "evidence": [
      "f_0e50e8"
    ],
    "measured": {
      "line": 32,
      "checks_on_target": 1
    },
    "threshold": {},
    "runtime_scope": {},
    "explanation": "O check roda, gasta uma passada sobre o dado, e o resultado não leva a lugar nenhum: não há `raise`, não há `assert`, não há saída com código de erro. O job termina verde com dado inválido, que é pior do que não validar — a suíte cria a crença de que há uma garantia, e é essa crença que faz ninguém procurar o problema quando ele aparece a jusante. `dq.enforcement` só é emitido quando a consequência está presente e é coerente; proteção pela metade — ler o resultado e apenas registrar em log — não emite, de propósito, porque chamar isso de consequência calaria a regra exatamente sobre o defeito que ela existe para achar. O que não pôde ser lido virou `dq.unresolved`, contado, e não conta como ausência de proteção. O RECORTE É O CORPUS ANALISADO, e o achado precisa ser lido assim: \"sem consequência neste corpus\". Consequência atrás de um helper (`aborta_se(ruins)`) não é vista — provar isso exige seguir o valor para dentro da função, o que esta análise não faz. Se a consequência está noutro módulo fora do recorte, o achado é um convite a verificar, não uma sentença. É a mesma disciplina que `unreferenced_function_count` passou a adotar na Fase 5b: declarar o recorte junto com o número. RESSALVA SOBRE `assert` (desvio D-5c-4). `assert` conta como consequência aqui, porque nenhuma fonte oficial da AWS mostra Glue ou EMR rodando o driver com `-O`, e no Glue o único mecanismo documentado de variável de ambiente do driver (`--customer-driver-env-vars`) exige o prefixo `CUSTOMER_`, que impede definir `PYTHONOPTIMIZE`. Sem fonte que sustente o contrário, vale o comportamento padrão do interpretador: `__debug__` é verdadeiro e o `assert` executa. Mas a linguagem é explícita — sob `-O` ou com `PYTHONOPTIMIZE` definido, o interpretador não gera código nenhum para `assert` —, então validação cuja ÚNICA consequência é um `assert` fica a uma variável de ambiente de não ter consequência nenhuma, e isso desaparece sem aviso. `-O` é fato de ambiente, e esta análise lê apenas o `.py`: `raise` com exceção explícita não depende de nenhuma dessas condições.",
    "proposed_change": [
      "Ler o resultado do check e abortar o job quando ele reprovar, com exceção explícita — `raise`, e não `assert`, quando a proteção precisa sobreviver a um interpretador otimizado.",
      "Se o job deve seguir com dado parcial de propósito, registrar a decisão em métrica ou log estruturado e declarar o contrato — validação que só observa precisa dizer que só observa, no código e para quem consome.",
      "Se a consequência já existe atrás de um helper ou noutro módulo, confirmar que ela roda neste caminho antes de mudar qualquer coisa — este achado lê apenas o corpus passado na linha de comando."
    ],
    "expected_effect": "",
    "benchmark_ref": "",
    "risks": [
      "Passar a abortar o job muda o comportamento de produção: pipeline que hoje termina verde passará a falhar quando o dado violar a regra. É o efeito pretendido, e precisa ser combinado com quem opera.",
      "Um limiar mal calibrado transforma a proteção em interrupção recorrente, e a resposta usual a isso é desligar a validação inteira — pior do que o estado inicial."
    ],
    "tradeoffs": [
      "Abortar cedo protege o consumidor e interrompe a carga; seguir e sinalizar preserva a carga e transfere o risco a jusante. As duas são posições defensáveis, e a que não é defensável é não escolher."
    ],
    "validation": [
      "Rodar com um lote que viole a regra e confirmar que o job falha com código de saída diferente de zero.",
      "Rodar com um lote válido e confirmar que a contagem total e a contagem por chave no destino não mudaram.",
      "Se a consequência for `assert`, rodar o mesmo lote inválido com `PYTHONOPTIMIZE=1` e observar que o job passa — é a prova de que a proteção é condicional ao ambiente."
    ],
    "rollback": [
      "Reverter o commit."
    ],
    "sources": [
      {
        "origin": "field-heuristic",
        "note": "P1 e não P0: o defeito é a garantia inexistente, não um dado comprovadamente inválido — o motor não sabe se o check reprovaria. A ressalva sobre `assert` e `-O` está apurada, com URL e data, em knowledge/dq/validation-frameworks.md seção 3."
      }
    ]
  },
  {
    "rule_id": "SF-DQ-003",
    "schema_version": 1,
    "catalog_version": 1,
    "title": "Validação recomputa o lineage",
    "severity": "P2",
    "confidence": "high",
    "status": "structural",
    "subject": {
      "type": "source_location",
      "file": "job.py",
      "line": 32,
      "col": 8,
      "symbol": "",
      "snippet": ""
    },
    "evidence": [
      "f_0e50e8"
    ],
    "measured": {
      "line": 32,
      "checks_on_target": 1
    },
    "threshold": {},
    "runtime_scope": {},
    "explanation": "Spark é lazy: a action do check materializa o DataFrame, e a próxima action sobre o mesmo DataFrame recomeça do último ponto materializado. Sem `cache`/`persist`, o lineage inteiro — leitura, joins, transformações — roda duas vezes, uma para validar e outra para escrever. O custo é proporcional ao lineage, não ao tamanho do check. OS DOIS ATRIBUTOS SÃO NECESSÁRIOS, e cada um sozinho não prova nada. Alvo não persistido sem reuso depois não recomputa coisa alguma — a action do check é a única que passa por ali. E reuso sobre alvo persistido não recomputa — é o caso correto, e é por isso que a fixture negativa desta área tem `action_after_check: true` sem disparar nada. O extrator só afirma `target_persisted: false` quando não viu `cache`/`persist` no escopo; quando o alvo não pôde ser resolvido, ele não inventa a chave, e `engine._where_matches` reprova caminho ausente. O RECORTE É O ESCOPO MAIS UM PASSO, e o achado precisa ser lido assim: \"não persistido neste escopo, nem no chamador quando ele é legível\". Quando o alvo do check chega por parâmetro e não há `cache`/`persist`/`unpersist` sobre ele dentro da própria função, o extrator vai ao chamador: se a função tem EXATAMENTE UM call site no mesmo arquivo, a chamada é por nome e o argumento é uma variável, a evidência daquele call site é herdada — `true` quando o chamador persistiu antes de chamar, `false` quando não persistiu. A forma canônica de biblioteca Glue é validar num helper e persistir no chamador, e essa forma agora é AVALIADA em vez de ignorada: quando a persistência existe, a regra vê e não dispara; quando não existe, ela dispara sobre um defeito que antes era invisível. OS LIMITES DA HERANÇA SÃO PARTE DO ACHADO, e onde ela não vale a chave continua ausente e esta regra não avalia o check: chamador em OUTRO módulo (o extrator lê um arquivo por vez), MAIS DE UM chamador no arquivo (um pode persistir e o outro não, e escolher um seria inventar), chamada por ATRIBUTO como `self.valida(df)` (o nome nu não prova qual função roda), argumento que não é uma variável, parâmetro religado dentro do helper antes do check, e nome que o próprio chamador não liga nem persiste — um global cacheado um escopo acima, cujo `false` acusaria um DataFrame que está em cache. Herança estende evidência, nunca acusação. O preço é declarado, não escondido: esta regra continua calada para o helper de biblioteca cujo chamador vive noutro arquivo, que é o caso mais comum de biblioteca compartilhada. Subnotificação é o lado aceito; acusar a forma canônica não é. É a mesma disciplina de recorte que `SF-DQ-002` declara para consequência atrás de um helper — e lá o recorte continua inteiro, porque provar aborto dentro do callee exige ler o corpo dele, que é travessia que esta fase não fez.",
    "proposed_change": [
      "Persistir o DataFrame antes do check, com o nível de storage adequado ao tamanho, e liberar com `unpersist` depois da última action.",
      "Alternativa sem cache: calcular a métrica de validação junto do resultado, numa única agregação, em vez de numa passada separada."
    ],
    "expected_effect": "",
    "benchmark_ref": "",
    "risks": [
      "Cache de DataFrame grande pressiona memória de executor e pode causar spill ou OOM — o remédio de um gargalo vira outro.",
      "`unpersist` esquecido mantém blocos ocupando memória e disco até o fim do job, e o efeito aparece longe da mudança."
    ],
    "tradeoffs": [
      "Persistir troca CPU e I/O de recomputo por memória; qual é melhor depende de qual recurso está saturado."
    ],
    "validation": [
      "Número de jobs Spark submetidos antes e depois, na Spark UI.",
      "Tempo de execução com o volume de produção.",
      "Contagem e schema do resultado, inalterados."
    ],
    "rollback": [
      "Remover o `cache`/`persist` e o `unpersist` correspondente, e reverter o commit."
    ],
    "sources": [
      {
        "url": "https://docs.aws.amazon.com/prescriptive-guidance/latest/tuning-aws-glue-for-apache-spark/optimize-shuffles.html",
        "retrieved": "2026-08-03",
        "note": "Secao 'Remove unneeded Spark actions': cada RDD transformado pode ser recomputado a cada action."
      },
      {
        "origin": "field-heuristic",
        "note": "P2 e nao P1: o defeito e custo, nao correcao -- o dado publicado esta certo, so foi pago duas vezes. Nenhum fact disponivel mede o tamanho do lineage recomputado."
      }
    ]
  }
]
